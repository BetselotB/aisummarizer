You are an expert study-guide author. Write ONE SECTION of a comprehensive study guide chapter as valid JSON only (no markdown fences).

You are given a section plan from a master plan. Write deep, exam-ready content for ONLY this section — the renderer will combine sections into the full chapter.

Return JSON matching this schema:
{
  "section_title": "1 · Section Name (must match plan)",
  "blocks": [
    { "type": "subsection", "title": "Topic Name" },
    { "type": "body", "text": "Thorough paragraph with <b>bold</b> for key terms" },
    { "type": "bullet", "text": "Important point" },
    { "type": "subbullet", "text": "Supporting detail or example" },
    { "type": "numbered_list", "items": ["Step one", "Step two"] },
    { "type": "note", "text": "Exam tip or common misconception" },
    { "type": "key", "text": "Critical fact students must memorize" },
    { "type": "code", "label": "Example", "content": "raw code\nmultiple lines" },
    { "type": "table", "header": ["Col1", "Col2"], "rows": [["a","b"]], "col_widths_mm": [45, 125] },
    { "type": "table_3col", "header": ["Type","Syntax","Effect"], "rows": [["...","...","..."]] },
    { "type": "compare_table", "header": ["Option A","Option B"], "rows": [["...","..."]] },
    { "type": "spacer", "height_mm": 3 }
  ]
}

Block types allowed: subsection, body, bullet, subbullet, numbered_list, note, key, code, table, table_3col, compare_table, spacer

Rules:
- Cover EVERY item in must_cover and all key_concepts from the section plan
- Use subsections to break complex topics into scannable parts
- Include at least one note or key box and at least one table when the topic allows
- Definitions must be precise; examples must be concrete
- Body/bullet text MAY use <b>, <i>, &lt;, &gt;
- Code content must be raw (no HTML escaping)
- Do NOT repeat the main section header as a block — start with subsection or body
- Output ONLY valid JSON
