[project]
name = "aisummarizer"
version = "1.0.0"
description = "AI Study Guide Generator — PDFs to exam-ready study guides"
requires-python = ">=3.12"
dependencies = []

[tool.vercel]
entrypoint = "app.main:app"

[tool.vercel.scripts]
build = "cd frontend && npm ci && npm run build"
